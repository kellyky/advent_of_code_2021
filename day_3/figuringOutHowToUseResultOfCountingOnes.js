// const fs = require("fs");

// const readings = fs
//   .readFileSync("sampleInput.txt", { encoding: "utf-8" })
//   .split("\n")
//   .filter((x) => x)

const readings = ['00', '11', '22', '33'];
const isIndexZero = arr => arr[0];

const length = readings.length;

let indexZero = Array(length).fill(0);
let indexOne = Array(length).fill( {

  for ( line of readings){
    'x';



  }


});


console.log(indexZero, indexOne);



// const something = readings.map(( el) => {
  // let index = 0;

  // el.filter(isIndexZero);


//   while (index < el.length){

//     let arr = readings.filter( el => {
//       for (let index of el){



//         return el[index];
//       }

//     })

//     index++;
//   return arr;
//   }
// })

